digraph Example1 {
  inputs = "xs";
  outputs = "fs";

  f[block_type="F"];
  for_each[block_type="For_each"];
  
  source:xs -> for_each:xs;
  for_each:x -> f:x;
  f:f -> for_each:f;
  for_each:fs -> stock:fs;
}
